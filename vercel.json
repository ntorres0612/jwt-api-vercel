{
    "version": 2,
    "builds": [
        {
            "src": "api/generate-token.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/generate-token",
            "dest": "api/generate-token.js"
        }
    ]
}